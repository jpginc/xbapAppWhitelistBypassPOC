<Page x:Class="powershell.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Page1">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ScrollViewer  Grid.Column="0" Grid.RowSpan="1" Grid.Row="0">
            <TextBox Name="in" Grid.Column="0" Grid.Row="0" Grid.RowSpan="1" 
            AcceptsReturn="True" TextWrapping="Wrap" />
        </ScrollViewer>
        <Button Grid.Column="0" Grid.Row="1" Grid.RowSpan="1" MaxHeight="100" Margin="0,10,0,0" 
                Click="Button_Click">run powershell</Button>
        <ScrollViewer  Grid.Column="1" Grid.RowSpan="2" Grid.Row="0">
            <TextBox Name="out"  Background="Black" 
                     Foreground="White" AcceptsReturn="True" TextWrapping="Wrap"   />
        </ScrollViewer>
    </Grid>
</Page>
